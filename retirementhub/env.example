# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=retirementhub
DB_USER=postgres
DB_PASSWORD=your_password_here

# Backend port (default 80 in container; mapped to 8100 in docker-compose)
PORT=80

# Copy to .env and set your values. .env is gitignored.

# Database Configuration
# PostgreSQL connection settings
DB_HOST=postgres
DB_PORT=5432
DB_NAME=hausfrau
DB_USER=postgres
DB_PASSWORD=your_password_here

# Backend Configuration
# Port for the backend service (default: 80, exposed as 8080 in docker-compose)
PORT=80

# Note: Copy this file to .env and update with your actual values
# The .env file is gitignored and should not be committed to version control
